<UserControl x:Class="NoiseBox_UI.View.UserControls.SongList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NoiseBox_UI.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="507" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ListViewStyle1" TargetType="{x:Type ListView}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="Foreground" Value="Black"/>
            <!--<Setter Property="ItemContainerStyle" Value="{Binding View, Converter={StaticResource MaterialDesignListViewItemContainerStyleConverter}, RelativeSource={RelativeSource Self}}"/>-->
            <Setter Property="OverridesDefaultStyle" Value="False"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <!--<Setter Property="Template" Value="{Binding View, Converter={StaticResource MaterialDesignListViewTemplateConverter}, RelativeSource={RelativeSource Self}}"/>-->
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <ListView Style="{DynamicResource ListViewStyle1}" Name="List" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="#" Width="auto" DisplayMemberBinding="{Binding Name}">
                        
                    </GridViewColumn>
                    <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Age}" />
                    <GridViewColumn Header="Path To File" Width="auto" DisplayMemberBinding="{Binding Mail}"/>
                    <GridViewColumn Header="Duration" Width="auto" DisplayMemberBinding="{Binding Mail}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
